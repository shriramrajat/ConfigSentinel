# FortiOS 7.0.2 Config Fixture
config system global
    set hostname "FORTIGATE-FW-01"
    set admin-sport 443
    set admin-ssh-port 22
    set admin-ssh-version v1
    set admintimeout 5
end
config system ntp
    set status disable
end
config system log syslogd setting
    set status disable
end
